# Fix — demo_sort

## Changes

- **`buggy.py:14`**: 将比较运算符从 `<` 改为 `>`。
  - 根因：升序冒泡排序应当在**前 > 后**时交换相邻元素，让较大的元素逐步"冒泡"到数组尾部。原代码写成了 `if arr[j] < arr[j + 1]`（前 < 后时交换），导致每轮冒泡把较小元素上浮到末尾，最终输出降序序列。
  - 这是唯一改动，文件其余内容不变。

## Self Check

- **覆盖根因**：是。比较符号是唯一的 bug，已修正为 `>`，符合升序冒泡排序的交换条件语义。
- **语法/逻辑风险**：无。修改前后代码结构一致，仅变了一个字符，不存在引入语法错误或新逻辑缺陷的可能。
- **边界情况**：空列表 `[]` 和单元素 `[1]` 在修复前已能通过（循环体不执行），修复后仍正确通过；重复元素 `[2,2,1]` 修复后也正确排序为 `[1,2,2]`。
- **回归风险**：无。仅修改了比较条件，函数签名、返回值类型、副作用特征均未改变。
- **验证结果**：运行 `python3 test_algo.py`，全部 5 个用例通过，退出码 0。
