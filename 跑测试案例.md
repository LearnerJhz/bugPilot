cd /Users/bytedance/person/githubBug/bugPilot && for m in core.ports core.models core.manifest core.registry adapters.agent_runner executors.intake executors.agent_executor executors.apply executors.verify; do echo "== $m =="; python3 -m $m 2>&1 | tail -n 2; done


验证CLI单阶段
cd /Users/bytedance/person/githubBug/bugPilot && echo "== only intake ==" && python3 -m cli run demo_cli --description "登录页空密码崩溃" --only intake; echo "exit=$?"; echo "== preview analyze ==" && python3 -m cli run demo_cli --only analyze --preview; echo "exit=$?"; echo "== status ==" && python3 -m cli status demo_cli; echo "exit=$?"; rm -rf tasks/demo_cli